# 리뷰 결정 기록: fix/snapshot-blank-render

계획: `docs/plans/2026-09-23-snapshot-blank-render.md`. 계획의 "결정 사항" 표도 확정 결정으로 본다.

## 확정 결정

| ID | 결정 | 이유 |
|---|---|---|
| D1 | 스냅샷은 `ImageRenderer.render(rasterizationScale:renderer:)`로 직접 만든 RGBA8 컨텍스트에 그린다. 허용 오차(1%)는 그대로 | CI 실패는 `uiImage`가 완전히 투명한 이미지를 돌려준 것이었다(차이 비율 = 기준 이미지 채널 평균). 문서화된 CG 경로로 옮긴다. 원인 확정이 아니라 가설임을 계획에 적어 두었다. 재발하면 D3 메시지와 첨부로 바로 구분된다 |
| D2 | 기준 이미지 21장을 모두 새 경로로 다시 기록한다 | 옛 기준과 새 렌더가 0.04~0.84% 체계적으로 달라(글리프 advance 차이) 1% 여유 대부분을 쓰고 있었다(R1-3). 한 경로로 통일해 여유를 되찾는다 |
| D3 | 렌더가 비고 기준은 비어 있지 않을 때만 "빈 렌더" 메시지로 실패한다. 재시도하지 않는다 | 재시도는 원인을 가린다. 기준도 투명하면 의도한 투명 뷰다(R1-5) |
| D4 | 실패 이미지는 Swift Testing 첨부 + 별도 아티팩트 `SnapshotFailures`로 올린다. xcresult 업로드 스텝과 합치지 않는다 | 경로가 여럿이면 upload-artifact가 공통 조상을 루트로 잡아 xcresult 번들이 `_temp/` 아래로 밀려 README의 "내려받아 Xcode로 연다"가 깨진다(R1-1) |

## 라운드 이력

### 1라운드

- 리뷰 기준 스냅샷: `4fd9d307a662ca2f496d3633a444e06e8c77ec8b`
- R1-1 반영: 업로드 스텝 분리(D4), README 한 줄.
- R1-2 반영: `pixelBytes`에서 `withExtendedLifetime(context)`로 복사 동안 컨텍스트를 붙잡는다.
- R1-3 반영: 허용 오차 0으로 21케이스를 재 여유를 확인하고 전량 재기록(D2). 계획 문서의 "안티앨리어싱만" 문장을 실측대로 고쳤다.
- R1-4 반영: `mismatch`를 internal로 풀고 실패 기록을 클로저로 주입. 빈 렌더 분기·양쪽 투명 분기 테스트 추가.
- R1-5 반영: D3.
- R1-6 보류: 기록 모드는 사람이 켜고 결과를 눈으로 보는 절차라 다음 실행에서 걸리는 것으로 충분하다.
- R1-7 반영: 위 빨강/아래 파랑 렌더로 픽셀 크기와 방향을 고정하는 테스트.
- R1-8 보류: 동작 영향 없음. 호출자가 모두 메인 액터다.
- R1-9 반영: 계획 API 절의 뒤집기 문장 삭제.
