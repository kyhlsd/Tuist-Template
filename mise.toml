# 팀 전원이 같은 버전을 쓰도록 고정한다. 버전을 올릴 때는 여기만 바꾼다.
# swiftformat·swiftlint 버전이 다르면 같은 코드도 포맷·린트 결과가 달라진다.
[tools]
tuist = "4.208.0"
swiftformat = "0.63.0"
swiftlint = "0.65.1"
# 생성기 버전이 다르면 같은 명세에서도 생성물이 달라진다. Scripts/openapi-generate.sh 가 쓴다.
"spm:apple/swift-openapi-generator" = "1.13.1"
